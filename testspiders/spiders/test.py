from scrapy.********* import Request

def _segrigate_urls(response):
    pr*********t "hello world"
    pr*********t response
    # if(response.headers only )
    #     if data:
    #         save url
    #     else:
    #         Request(response.url, callback=self.parse)


url="*********://*********.urmobi.*********"

Request(url, callback=_segrigate_urls)
